name: Release And Update Registry

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Release tag to publish (for example v0.2.0)"
        required: true

jobs:
  release:
    uses: Charles546/honeydipper-registry/.github/workflows/release-driver.yml@main
    with:
      driver_name: hd-driver-podman
      binary_name: hd-driver-podman
      go_build_path: ./cmd/hd-driver-podman
      release_tag: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}
      manifest_file: hd-driver-podman.json
      mirror_url: https://charles546.github.io/honeydipper-registry/artifacts/hd-driver-podman/${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_tag || github.ref_name }}/linux-amd64/hd-driver-podman
    secrets:
      registry_repo_token: ${{ secrets.REGISTRY_REPO_TOKEN }}
